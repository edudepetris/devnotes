<%= react_component("SignInScreen", {
  resource: resource.serializable_hash,
  signInPath: session_path(resource_name),
  signUpPath: new_registration_path(resource_name),
  resetPasswordPath: new_password_path(resource_name),
  errorMessage: flash.alert || '',
  authenticityToken: form_authenticity_token,
  infoMessage: flash.notice || '' }) %>
