<%= react_component("ChangePasswordScreen", {
  resource: resource.serializable_hash,
  resetPasswordToken: resource.reset_password_token,
  signInPath: new_session_path(resource_name),
  signUpPath: new_registration_path(resource_name),
  errorMessages: resource.errors.full_messages,
  passwordPath: password_path(resource_name),
  authenticityToken: form_authenticity_token}) %>
